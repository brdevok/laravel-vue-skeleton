import type { StoreOptions } from 'vuex';
import type { __name__(pascalCase)State } from './__name__(kebabCase).types';

export default ((): StoreOptions<__name__(pascalCase)State> => ({
	state() {
		return {};
	},
	getters: {
		// Create your getters here or remove.
	},
	mutations: {
		// Create your mutations here or remove.
	},
	actions: {
		// Create your actions here or remove.
	},
}))();
